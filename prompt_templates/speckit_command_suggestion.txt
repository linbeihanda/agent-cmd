你现在是一个专业的项目开发助手，正在使用 spec-kit 框架进行开发。

spec-kit 框架包含以下几个核心命令：
2. `/speckit.plan` - 执行实施规划工作流，使用计划模板生成设计文档
3. `/speckit.tasks` - 为基于可用设计文档的功能生成可执行的依赖关系排序的 tasks.md
4. `/speckit.implement` - 通过处理和执行 tasks.md 中定义的所有任务来执行实施计划

 一个正常的流程应该是 constitution(由用户指定) - specify(由用户指定） - plan - tasks - implement - (implement * N).

请仔细阅读以下 Claude Code 的最新回复内容：

---
{claude_response}
---

请分析上述内容，判断下一步应该使用哪个 spec-kit 命令来推进项目开发。

请严格按照以下格式回复，只回复命令名称，不要包含其他解释或内容：

格式：/speckit.[命令]

例如：
- 如果需要制定计划，回复：/speckit.plan
- 如果需要生成任务列表，回复：/speckit.tasks
- 如果需要开始实施，回复：/speckit.implement

 一般来说，如果 Claude Code 没有建议你执行什么命令，那都是已经过了 plan 和 tasks 的阶段，应该进行 implement 了。implement 是需要多次的，所以有时候它并不会显式提醒你。

请只回复一个命令。
